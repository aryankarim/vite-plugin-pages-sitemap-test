export declare const generateSitemapXml: ({
  routes,
  hostname,
  otherConfig,
}: {
  routes: Array<{
    name: string;
    path: string;
  }>;
  hostname: string;
  otherConfig?: any;
}) => {
  name: string;
  configResolved(): void;
};
